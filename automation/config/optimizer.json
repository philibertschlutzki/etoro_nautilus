{
  "_schema": {
    "description": "Konfiguration f\u00fcr den Hyperparameter-Optimierer.",
    "fields": {
      "n_trials": "Anzahl der Optimierungsdurchl\u00e4ufe (Trials) pro Strategie.",
      "n_startup_trials": "Anzahl der initialen Zufalls-Trials f\u00fcr den Optuna Sampler.",
      "seed": "Random Seed f\u00fcr Reproduzierbarkeit.",
      "penalty_overfit_weight": "Gewichtung der Strafe f\u00fcr Overfitting.",
      "penalty_dd_weight": "Gewichtung der Strafe f\u00fcr Drawdowns.",
      "bonus_coverage_weight": "Gewichtung des Bonus f\u00fcr Coverage.",
      "penalty_unevaluable_oos": "Strafe, wenn OOS nicht auswertbar ist.",
      "sortino_clip_abs": "Maximaler absoluter Wert f\u00fcr die Sortino Ratio beim Capping.",
      "unevaluable_shaping_span": "Spanne f\u00fcr das Reward-Shaping von nicht-evaluierbaren Trials (z. B. 0.25).",
      "constraint_distance_penalty_weight": "Issue #452 - Gewicht fuer kontinuierliche, quadratische Distanz-Penalties bei evaluierten, aber nicht eligiblen OOS-Trials. Ersetzt Flat-Floor-Clamping fuer Constraint-Verletzungen durch einen TPE-sichtbaren Near-Miss-Gradienten, bleibt aber strikt unter dem Evaluable-Floor.",
      "evaluable_floor_epsilon": "Kleines Epsilon, um zu garantieren, dass evaluierbare Trials immer besser als nicht-evaluierbare sind.",
      "shaping_trade_target": "Summe der IS-Trades \u00fcber das Universum, bei der das IS-Aktivit\u00e4ts-Shaping nicht-evaluierbarer Trials saturiert (ISSUE-OPT-375). Konservativ w\u00e4hlen: rein zur Gradientenbildung Richtung Eligibility, \u00e4ndert nie die Floor-Invariante (Shaping bleibt <= unevaluable_shaping_span).",
      "per_symbol_shaping_trade_target": "Issue #406 (Pitfall #75) \u2014 dedizierter Saettigungs-Target f\u00fcr den Per-Symbol-Pfad (universe_size==1 ODER reward_mode=='per_symbol'). shaping_trade_target ist universe-skaliert (~70 Symbole) und saettigt im Per-Symbol-Pfad sofort (is_total_trades = Fold-Summe EINES Symbols \u226b 50) \u21d2 Zero-Gradient. Dieser gr\u00f6\u00dfere Wert (z. B. 400) h\u00e4lt activity = is_total_trades / target im Per-Symbol-Pfad unges\u00e4ttigt. Fehlt der Key \u21d2 Fallback auf shaping_trade_target (Legacy). \u00c4ndert NIE die Floor-Invariante (Shaping bleibt <= unevaluable_shaping_span).",
      "shaping_return_target": "Issue #407 (Pitfall #75) \u2014 Normierungs-Target f\u00fcr die IS-Rendite-Komponente der Gate-Naehe (_gate_proximity). is_best_total_return (beste IS-Rendite, fraktional, z. B. 0.5 = 50%) wird auf min(1, is_best_total_return / shaping_return_target) geclippt. Rein zur Gradientenbildung Richtung Eligibility f\u00fcr unevaluable Trials; performance-basiert (kein Gate-Flag \u21d2 kein Gate-Gaming). \u00c4ndert NIE die Floor-Invariante (Shaping bleibt <= unevaluable_shaping_span). Fehlt der Key \u21d2 Komponente inaktiv.",
      "shaping_winrate_target": "Issue #407 (Pitfall #75) \u2014 Normierungs-Target f\u00fcr die IS-Trefferquoten-Komponente der Gate-Naehe (_gate_proximity). is_best_win_rate (beste IS-win_rate \u2208 [0,1]) wird auf min(1, is_best_win_rate / shaping_winrate_target) geclippt. Rein zur Gradientenbildung Richtung Eligibility; performance-basiert. \u00c4ndert NIE die Floor-Invariante. Fehlt der Key \u21d2 Komponente inaktiv.",
      "lambda_reg": "A4.3 \u2014 St\u00e4rke der Shrinkage-Strafe param_pen im Per-Symbol-Reward (universe_size==1). param_pen = lambda_reg * normalized_param_distance(sampled, global_params). KALIBRIERUNG: konservativ w\u00e4hlen \u2014 sehr gro\u00dfe param_pen kollabieren Trials auf den Floor (TPE unterscheidet geflorte Trials nicht); nahe dem globalen Optimum bleibt der Gradient erhalten.",
      "promotion_margin": "A4.5b \u2014 Marge, die der symbol-getunte Vektor das globale Baseline auf dem ungesehenen Holdout schlagen muss, damit ein instrument_override promotet wird (Gate 3).",
      "reward_mode": "A4.3 \u2014 'auto' (Per-Symbol-Reward nur bei universe_size==1) | 'per_symbol' (erzwingt den Per-Symbol-Pfad auch bei universe_size>1).",
      "oos_sortino_fallback": "Issue #401 \u2014 Fallback-Metrik fuer den Reward, wenn ein OOS-Sample evaluiert UND eligible ist, der Sortino aber mathematisch undefiniert bleibt (losses_count == 0 oder n < sortino_min_trades). 'total_return' \u21d2 geclippter OOS-total_return als evaluable Base (verhindert Kollaps auf den -9.75-Floor / Flat-Reward-Landscape); null/absent \u21d2 Legacy-Penalty-Pfad. Der Reward-WERT bleibt rein performance-basiert (kein Gate-Flag) \u2192 kein Gate-Gaming; Micro-Sizing-/Risiko-Gates bleiben ueber oos_eligible wirksam.",
      "gate1_buffer_days": "A4.4 \u2014 Puffer (Tage) \u00fcber IS + splits*OOS + Holdout, den die Symbol-Historie zus\u00e4tzlich abdecken muss (Gate 1 Daten-Suffizienz).",
      "min_bars_per_param": "A4.4 \u2014 Mindestverh\u00e4ltnis available_bars / n_params (Anti-Overfit-Heuristik in Gate 1).",
      "min_oos_bars_per_fold": "A4.4 \u2014 Mindest-Bar-Zahl je OOS-Fold (oos_window_days * bars_per_day), damit ein Fold statistisch aussagekr\u00e4ftig ist (Gate 1).",
      "storage_url": "A4.7 (optional) \u2014 Optuna-Storage-URL f\u00fcr Per-Symbol-Studies. null \u21d2 SQLite-Default ({WORK}/sweep/{study}.db). Reines Opt-In f\u00fcr echte Multi-Maschinen-Parallelit\u00e4t (z. B. PostgreSQL); weicht die SQLite-Leitplanke (Pitfall #53) bewusst auf. ENV ETORO_OPTUNA_STORAGE hat Vorrang. Bei non-SQLite nur n_jobs=1 deterministisch.",
      "reward_semantics_version": "Issue #410 (Pitfall #75) \u2014 monoton steigende Versionsnummer der Reward-Semantik. Wird beim Anlegen einer frischen Study als user_attr gestempelt. Traegt eine geladene Study eine ALTE/keine Version, obwohl sie schon Trials hat, warnt der Optimizer laut (alte Floor-Trials sind mit der neuen Semantik nicht vergleichbar \u21d2 stale DB loeschen). v3 = Fixes #404\u2013#407 (Per-Symbol-Evaluierbarkeit entkoppelt, kontextsensitives Shaping, Gate-Proximity-Gradient)."
    }
  },
  "n_trials": 100,
  "n_startup_trials": 16,
  "seed": 42,
  "penalty_overfit_weight": 0.5,
  "penalty_dd_weight": 8.0,
  "bonus_coverage_weight": 1.0,
  "penalty_unevaluable_oos": -10.0,
  "sortino_clip_abs": 5.0,
  "unevaluable_shaping_span": 0.25,
  "constraint_distance_penalty_weight": 0.25,
  "evaluable_floor_epsilon": 0.001,
  "shaping_trade_target": 50,
  "per_symbol_shaping_trade_target": 400,
  "shaping_return_target": 0.5,
  "shaping_winrate_target": 0.5,
  "lambda_reg": 0.25,
  "promotion_margin": 0.10,
  "reward_mode": "auto",
  "oos_sortino_fallback": "total_return",
  "gate1_buffer_days": 30,
  "min_bars_per_param": 200,
  "min_oos_bars_per_fold": 500,
  "storage_url": null,
  "reward_semantics_version": 3
}

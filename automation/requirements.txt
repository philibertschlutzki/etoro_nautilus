# Core
aiohttp>=3.9.0
websockets>=12.0
# Issue #852 — Parquet-Rundungsverhalten hat numerische Wirkung auf die Backtest-Eingabedaten;
# obere Grenze ergaenzt (Untergrenze 16.0 unveraendert), analog pandas/optuna/numpy unten.
pyarrow>=16.0,<26.0
python-dotenv>=1.0.0

# Nautilus (für backtest_runner.py und strategies/)
# Issue #852 — obere Grenze ergaenzt: die Nautilus-Basisklasse (HourlyStrategyBase, #836/#837)
# haengt an der Order-/Position-Callback-Semantik, die sich zwischen Majors aendern kann.
nautilus_trader>=1.226.0,<2.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-json-report>=1.5.0
pytest-timeout>=2.3.0
pytest-mock>=3.12.0

# Issue #1105 — automation/ai_loop/validator.py::StaticValidator.run_linters scopes both tools
# to a SINGLE AI-Loop candidate file under automation/ai_loop/workspace/candidates/ (never the
# whole repo/automation package) via the config files in automation/ai_loop/ (ruff.toml/mypy.ini).
ruff>=0.6.0
mypy>=1.10.0

# Utils
# Issue #802 — pct_change()'s fill_method-Default aenderte sich zwischen 2.0/2.1/3.0 und liess die
# #756/#801-Log-Return-Identitaet von der Installationsumgebung statt der Konfiguration abhaengen.
# Untergrenze 2.2 (fill_method=None ist dort bereits Default, kein deprecation warning); obere
# Grenze offen bis zur naechsten major-Version-Pruefung.
pandas>=2.2,<4.0
# Issue #852 — numpy ist transitiv ueber pandas gepinnt, aber eigenstaendig relevant fuer
# np.log/np.diff/RNG-Verhalten (deflation.py, backtest_runner._calculate_stats); Bereich deckt die
# aktuell verifizierten 1.26.x/2.x-Serien ab.
numpy>=1.26,<3.0
requests>=2.25.0
# Issue #852 (P2) — Root-Cause: derselbe Kommentar oben (pandas/#802) gilt eins zu eins fuer Optuna:
# TPESampler-Defaults (multivariate/group/constant_liar), die constraints_func-Interaktion (#612)
# mit n_startup_trials, und die TrialState.PRUNED-Semantik (#845-relevant) sind nicht ueber Majors
# garantiert. Ohne Pin haengt der numerische Ausgang der Selektion an der Installationsumgebung.
optuna>=4.9,<5.0
